# Frontend backend selector
VITE_BACKEND_MODE=supabase

# Used when VITE_BACKEND_MODE=mysql_api
VITE_MYSQL_API_BASE_URL=http://localhost:8080

# Optional external participant auth (already supported)
VITE_LDAP_AUTH_URL=
